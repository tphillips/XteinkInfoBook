services:
  api:
    dns:
      - 8.8.8.8
    build:
      dockerfile: Dockerfile
    container_name: xteinkInfo
    environment:
      ASPNETCORE_URLS: http://+:8080
      XTEINK_HOST: 192.168.4.38
    volumes:
      - ./content:/content
    ports:
      - "5246:8080"
    restart: unless-stopped